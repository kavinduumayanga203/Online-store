package com.example.demo;

/**
 * Legacy placeholder kept for compatibility with some IDE setups.
 * The active Spring Boot application class is `com.store.OnlineStoreApplication`.
 */
public class OnlineStoreApplication {
	// Intentionally left without @SpringBootApplication so the build
	// detects a single main class located in com.store package.
}
